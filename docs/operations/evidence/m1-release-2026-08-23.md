# M1 production release evidence — 2026-08-23

## Decision

Production deployment completed for the reviewed candidate, but the milestone gate is **not accepted**. `GATE-M0` remains open because this run did not archive a fresh GitHub OAuth callback, disabled-contributor session rejection, or Durable Object cross-activation read. `GATE-M1` therefore remains unchecked.

No source bodies, answer bodies, cookies, OAuth codes, authorization headers, secret values, member emails, or full callback URLs are stored here.

## Candidate and authorization

| Field | Evidence |
| --- | --- |
| Candidate commit | `843f43ac11cd08f8c98db38470d78706fd8f928f` |
| Worker | `memory-garden-agent` |
| Custom domain | `https://memory.crgmhrc.asia` |
| D1 database | `memory-garden-control-plane` |
| Operator authorization | Explicit production authorization received in the release task |
| Working tree at release | Clean |
| `0004` SHA-256 | `ebda7d5e04fbded4a2503c28a44160325fefcaef4b354a8e25865d68f1ec81bb` |

## Backup and migration

The native whole-database Wrangler export was attempted first and Cloudflare rejected it because the database contains FTS5 virtual tables (`cannot export databases with Virtual Tables (fts5)`). Migration was blocked until a successful logical backup was produced.

The approved fallback logical backup exported all 18 application tables, including `chunks` (the authoritative source for rebuilding FTS), while excluding only FTS5 virtual/internal tables. The backup remained outside the repository in a mode-700 temporary directory; each SQL file was mode 600. Total logical backup size: **9,822 bytes**. Only file sizes and SHA-256 values were recorded in the restricted operator shell; no row content was copied into this repository.

| Check | Result |
| --- | --- |
| Pre-`0004` ledger | PASS — `0001`, `0002`, `0003` in order |
| Legacy `review_pending` guard | PASS — count `0` |
| Forward migration | PASS — `0004_m1_gate_completion.sql` applied once |
| Post-`0004` ledger | PASS — `0001`, `0002`, `0003`, `0004` in order |
| Reverse migration or data deletion | Not performed |

## Version upload and deployment

One strict version was uploaded with the seven-secret bundle. Wrangler inspection showed the seven secret names, `DB`, `KNOWLEDGE`, `AI`, `ASSETS`, and `ALLOW_INSECURE_LOCAL=false`; secret values were hidden.

| Field | Evidence |
| --- | --- |
| Uploaded version | `23c65c9e-108f-4c54-9a22-e4e1b62fa033` |
| Deployment | PASS — exact version at 100% |
| Deployment status | `23c65c9e-108f-4c54-9a22-e4e1b62fa033` at 100% |
| `workers_dev` / `preview_urls` | Checked-in config is `false` / `false`; workers.dev probe timed out and is not treated as account-level proof |

## Local gates before release

- `test:m1`: PASS — 30 operations contracts, 13 i18n contracts, 21 files / 563 tests.
- `npm run check`: PASS — 38 smoke/contracts, 13 i18n tests, 621 unit tests, 290 Workerd tests, types, vendor digests, and Wrangler dry-run build.
- `npm audit --omit=dev`: PASS — 0 vulnerabilities.
- Migration, documentation, i18n, type, and diff checks: PASS.

## Production smoke

Signed automation smoke against the custom domain passed. The smoke created one `smoke-` note; Phase 1 has no deletion API, so it remains for later recovery-bin cleanup.

| Check | Status | Redacted request ID |
| --- | --- | --- |
| Health | 200 | `a2f5f5602ac60b54` |
| Create note | 201 | `a2f5f5654b5c7a4b` |
| List notes | 200 | `a2f5f56dcb1e0b54` |
| Search | 200 | `a2f5f5795a460b54` |
| Grounded chat | 200 | `a2f5f59d29040b54` |
| Invalid signature health | 401 | `sha256-064529921d8a` |
| Automation admin route | 403 | `sha256-7ef9d6720554` |

The custom-domain root probe returned 200 with request ID `a2f5fb5539c261f9`. No workers.dev success was observed; the timeout is recorded as inconclusive rather than as proof of disablement.

## OPS-015 D1 cost evidence

These are production, read-only representative SQL probes with bounded limits. Results contain no row data. All probes returned `rows_written=0`.

| Operation | Returned rows | `rows_read` | `rows_written` |
| --- | ---: | ---: | ---: |
| Knowledge list, first page | 0 | 0 | 0 |
| Knowledge list, cursor page | 0 | 1 | 0 |
| FTS search, first page | 0 | 1 | 0 |
| FTS search, cursor page | 0 | 1 | 0 |
| Own submissions | 0 | 3 | 0 |
| Admin review queue | 0 | 0 | 0 |
| Same-space Tag list, first page | 0 | 2 | 0 |
| Same-space Tag list, cursor page | 0 | 2 | 0 |
| Revision/history detail | 0 | 2 | 0 |
| Original download/chunk path | 0 | 2 | 0 |
| Chat all-scope resolution | 0 | 0 | 0 |
| Tag AND/OR path | 0 | 1 | 0 |
| Bounded recovery scan | 0 | 2 | 0 |

The production D1 currently has no visible knowledge/revision rows, so every bounded probe returned zero rows. Local EXPLAIN/index assertions remain the plan evidence; no row contents or SQL result bodies are archived here.

## Free-plan review

The release remains within the private 5–20-person boundary: bounded request bodies/pages, keyset pagination, SQLite-backed Durable Object, D1 indexes, and provider-free refusal paths. Current official references reviewed on release day:

- D1 pricing and rows-read/written metrics: <https://developers.cloudflare.com/d1/platform/pricing/>
- Durable Objects pricing/free-plan behavior: <https://developers.cloudflare.com/durable-objects/platform/pricing/>
- Workers limits: <https://developers.cloudflare.com/workers/platform/limits/>
- Workers AI pricing: <https://developers.cloudflare.com/workers-ai/platform/pricing/>

## Remaining gate work

- Archive a successful GitHub OAuth callback on the deployed version.
- Archive a real disabled-contributor session rejection.
- Archive a cross-activation Durable Object read.
- Confirm workers.dev and preview URL disablement in the target account/Dashboard, not only local configuration.
- After independent review of those rows, decide whether `GATE-M0` and `GATE-M1` may be checked.

**Current decision: M1 implementation deployed; remote verification incomplete.**
