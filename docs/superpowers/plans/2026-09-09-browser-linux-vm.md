# 浏览器内 Linux 工作空间 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 本任务按用户要求在当前分支顺序执行，不自行创建其他任务或调用子代理。

**Goal:** 完成工作台内真实的浏览器 Linux：个人/临时环境、受控联网、终端与文件、保存恢复、用户隔离、知识库提交和完整验收。

**Architecture:** Linux 在访问者的浏览器执行，Cloudflare 管理成员所属的元数据与授权，不运行 Linux。独立 Runtime、加密本地存储、文件桥接和 WISP 中继适配通过类型契约连接；先用真实引擎验证，再接入管理页面。

**Tech Stack:** TypeScript、React/shadcn、v86（候选固定版 0.5.458，实施前核对包内容）、Web Worker、IndexedDB/Web Crypto、Cloudflare Worker/D1、受控 WISP。

**Spec:** `docs/superpowers/specs/2026-09-09-browser-linux-vm-design.md`

## Global Constraints

- 两种类型都必须完成；不以仅离线、模拟 shell、JavaScript 沙箱或远程云 VM 替代目标。
- 每个私有查询/修改由认证主体派生 `member_id`，跨用户资源不可见；列表采用 20/50/100 完整数字页码。
- 同操作 ID、相同请求返回原结果；同 ID、不同请求冲突；客体命令不自动重放。
- 当前浏览器配置文件最多一个运行实例；个人环境有界检查点，临时环境不保存完整快照。
- UI 沿用 shadcn、中英文、深浅主题、焦点/弹出层和独立滚动。
- VM 不进入匿名首页首屏依赖；私有文件不进入公共缓存、D1 或日志。
- 不读取/上传 `SECRETS_FILE`，不推送、部署、开通公共代理或购买资源。
- G0 未通过，不开始后续正式页面接入；中继资源授权缺失不得改报整体完成。
- 每一任务按失败测试 → 最小实现 → 正向/反向测试 → 记录证据推进；最终所有 VM-001～038 逐项验收。

### 2026-09-10 独立 G1a 基础切片进度

G0 仍未通过，正式页面与依赖客体运行的集成不提前开放。为避免重复的公网下载诊断阻塞不依赖联网的工作，本地先执行独立元数据切片；这不是 G0 放行，也不是 G1a 全部完成。原子步骤及测试证据见 `docs/superpowers/plans/2026-09-10-browser-linux-vm-metadata.md`。

- [x] 两种类型的创建、详情和 20/50/100 数字分页 API；认证主体派生成员归属，跨成员不可见。
- [x] 独立 `workspace.vm` bit 21；默认角色不补权，任务权限不推导 VM 权限，暂无菜单/页面入口。
- [x] 创建操作摘要及事务内原结果回执；并发重复只建一条，不同内容冲突，写入失败回滚。
- [x] 创建时仅能关联自己的任务；任务删除解除关联，不删除环境；旧创建回执不随任务删除而改变。
- [x] 元数据 PATCH/CAS：只更新提交字段，旧版本冲突；任务删除解除关联同时更新版本。
- [x] 创建、更新、删除统一成员级操作回执；并发重试只执行一次，跨操作/目标复用冲突。版本化删除保留最小墓碑，旧回执重放不恢复记录。
- [x] 当前成员的元数据操作历史 20/50/100 数字分页，事件序号稳定排序，不返回回执正文；已删除/他人环境历史不可访问。
- [x] 本地 59 项环境测试及 140 项现有相关测试通过，类型检查通过；0038→0039 旧回执兼容测试通过，仅测试数据库迁移。
- [x] 运行事件上报与代次去重、成员所属删除标记分页，以及独立本地清理协调器；2026-09-10 12:58 本地 250 项相关回归通过，类型检查通过。详见 metadata 子计划 Task 4；上报记录不代表实时状态，协调器尚未挂载真实存储。
- [ ] 真实运行控制器/加密存储接入、设备副本清理与退出/锁联动、容量约束和权限/导航启用验收。元数据及上报历史不是完整运行日志，G0 尚未通过。
- [ ] G1b～G5 的加密持久化、账户/运行控制、正式网络授权、文件与知识库闭环、shadcn UI 和发布验收仍按原范围推进。

## 交付分解与依赖

这份总计划保留完整范围；G0 是本轮立即执行的详细阶段计划。后续各阶段开始前，在同目录补充其文件级测试与实现步骤，不能用下面的分解表代替代码和测试，也不能因 G0 完成就结束总目标。

| 顺序 | 可独立验收交付 | 文件边界 | 规格验收 |
| --- | --- | --- | --- |
| G0 | 真实 Linux + 文件/快照/网络验证工具 | `scripts/browser-vm-*`、`tools/browser-vm/`、`design/browser-vm/` | VM-001～006 |
| G1a | 成员所属元数据、权限、幂等 API | `shared/environments.ts`、`src/environments/{types,repository,service,routes}.ts`、新追加 D1 migration、`src/app.ts`、权限位图 | VM-007～010 |
| G1b | 密钥授权、加密存储和备份 | `src/environments/keys.ts`、`frontend/features/environments/storage/{crypto,checkpoints,backup}.ts` | VM-011～012、020～021 |
| G2 | 运行控制器和账户生命周期 | `frontend/features/environments/runtime/{controller,worker,protocol,locks}.ts` | VM-013～019 |
| G3 | 联网授权、中继、SSRF、预算 | `src/environments/network.ts`、`tools/browser-vm/relay/`、`frontend/features/environments/runtime/network.ts` | VM-022～027 |
| G4a | 文件桥接及提交审核 | `frontend/features/environments/files/`、`frontend/lib/environments-data.ts`、现有 submissions API 适配 | VM-028～030 |
| G4b | 环境页面及全局入口 | `frontend/pages/environments/`、路由/导航能力表、locale 文件、专业终端组件 | VM-031～032 |
| G5 | 镜像分发、构建隔离和真实验收 | `scripts/browser-vm-assets.mjs`、`vite.config.ts`、CSP 定点修改、构建与浏览器测试、交付账本 | VM-033～038 |

## G0 接口与文件责任

`scripts/browser-vm-artifacts.mjs` 只负责验证输入的版本化引擎/镜像清单和本地文件摘要；不下载、不启动、不执行脚本、不修改业务文件。`scripts/browser-vm-artifacts.test.mjs` 使用临时目录和真实字节验证它，所有临时数据由测试自行清理。

```ts
type Artifact = {
  role: 'engine' | 'wasm' | 'bios' | 'vga-bios' | 'kernel' | 'rootfs';
  path: string; // 相对于明确提供的资源根，禁止绝对路径、目录穿越及越界符号链接
  bytes: number;
  sha256: string; // 64 位小写十六进制
  source: string; // https 来源，不包含认证信息
  license: string; // 明确声明，不能是空白
};
type Manifest = {
  schemaVersion: 1; engineVersion: string; imageVersion: string;
  rootfsMode: 'embedded' | 'external'; artifacts: Artifact[];
};
// embedded 不接受单独 rootfs 角色；external 必须包含 rootfs，不能漏报磁盘。
// 验证失败抛出可定位到具体资源的错误，任何必需角色缺失都不成功。
verifyArtifacts(manifest: unknown, root: string): Promise<{ artifacts: number; bytes: number }>;
```

引擎/API 获取后才确定 `tools/browser-vm/probe.mjs` 的真实构造参数。探针必须调用真实 v86，对 Linux 串口执行固定、不涉及用户文件的测试命令；不做 echo 成功标志替代内核启动。真实镜像和二进制放在明确的开发资源目录，文件摘要写入清单，许可证同时归档；不得未经审核提交大型二进制。

### Task 1：建立可复核资源输入

- [x] 先写 `scripts/browser-vm-artifacts.test.mjs`。用测试自己的临时目录创建 6 个角色的文件，独立计算真实字节 SHA-256，验证缺角色、错误大小、摘要不符、路径越界、外部符号链接、重复路径、非 HTTPS 来源和无许可证均被拒绝。关键测试形状：

```js
await assert.rejects(verifyArtifacts({ ...manifest, artifacts: manifest.artifacts.slice(1) }, root), /engine/);
await writeFile(join(root, 'engine.js'), 'tampered');
await assert.rejects(verifyArtifacts(manifest, root), /engine|size|digest/);
```

- [x] 运行 `rtk proxy node --test scripts/browser-vm-artifacts.test.mjs`，记录缺少验证器的失败，再实现 `verifyArtifacts`：严格对象/字段校验、角色完整性、真实路径边界校验、逐文件计数及 SHA-256 比对。
- [x] 再跑同一测试；加入正确输入用例，断言返回真实总字节数及 6 个资源，而不是检查源码字符串。
- [ ] 从 v86 官方发布源取得固定版本包、BIOS 和 Linux 资源；记录来源、许可证与摘要。若某项不可取得，记录具体失败，不造文件或标记资源完整。
  - [x] 开发探针已取得固定 v86、BIOS、Buildroot 内核和 Alpine 3.24.1 x86 根文件系统，记录实际字节和 SHA-256；Alpine 摘要与官方发布清单一致。
  - [x] 同日取得官方 Alpine virt 3.24.1 x86 ISO，完整字节与官方 SHA-256 一致；本地一次性实验真实启动 Linux 6.18.35-0-virt，并验证 `/mnt/work` 双向文件及新实例恢复。不是正式镜像接入或浏览器整套镜像验收。
  - [x] 后续完整 ISO 已接入维护中的 Node/浏览器探针：六项启动资源按实际大小与 SHA-256 验证，WASM 从已验证字节编译，BIOS/内核/initramfs/只读 ISO 以已验证字节交给每个新实例。资源许可和正式 JS 发布清单仍未验收。
  - [ ] 完整许可证/源码清单、可复现构建和拟发布镜像维护验收尚未完成。开发资源不等于可发布资产。
- [ ] 对拟发布资源运行验证器；摘要/大小/许可证都齐全才允许正式集成。个人开发用的官方演示镜像可先作 Task 2 兼容性探测，但必须注明未获生产资产验收，不得复制进公共静态目录或勾选 VM-001。

### Task 2：真实启动、文件和完整检查点

- [x] 读取固定版本导出的 API，写探针测试：在上限超时内没有真实串口提示符必须失败，不能因为 `emulator-ready` 事件就成功。
- [ ] 实现独立探针入口 `tools/browser-vm/probe.mjs`，通过显式参数读 Task 1 验证的资源，设置 256 MiB 客体内存并监听串口。
  - [x] 真实探针已可运行；显式开发目录、256 MiB、真实串口命令及非零退出码已验证。
  - [x] 增加显式 `alpine-iso` 选项及 ISO 目录；错误资源、未知选项或与 chroot 冲突均拒绝，不能静默退回旧镜像；真实测试校验 Alpine 自己的 Linux 6.18.35-0-virt 内核。
  - [ ] 接入最终通过许可和完整资源验证的发布清单；现阶段仅使用开发资源。
- [ ] 在 Linux 内执行 `uname -m`、`cat /proc/version`、证书路径检查和 `printf 'vm-roundtrip' > /mnt/work/probe.txt`；宿主文件桥读取的实际内容必须等于 `vm-roundtrip`。
  - [x] `/mnt` 共享目录已真实完成宿主→客体、客体→宿主读写；内核、架构、UTC 时间、工具和 CA 路径均有实际输出。
  - [x] 完整 ISO 在串口真实登录后挂载 `/mnt/work`；Node 与浏览器均验证宿主→客体、客体→宿主文件及新实例恢复。
  - [x] 本地离线诊断页接入真实串口人工输入：UTF-8、输入字节限额、无重发、受限输出缓冲、Ctrl+C、停止及重启；实际浏览器中文文件和运行中命令中断通过。
  - [ ] 专业终端模拟器、正式产品文件桥及账户/跨标签生命周期继续补齐；诊断页不等于正式终端。
- [ ] 写入另一文件、暂停并保存完整状态/磁盘/共享文件；销毁引擎，建立全新实例恢复，验证文件和 shell 可交互。漏掉任一数据部分的恢复必须失败或明确拒绝，不输出恢复成功。
  - [x] RAM 根文件、内存 9p 文件、Alpine 用户空间与 apk 在销毁原实例后恢复成功。
  - [x] 开发快照加入 SHA-256、长度/512 MiB 上限及声明的引擎/镜像/内存/文件系统匹配检查；8 项新测试验证损坏、不匹配和校验期间修改被隔离；Node 和浏览器真实恢复回归通过。
  - [x] 完整 ISO 的检查点镜像身份绑定六项已验证启动资源摘要，恢复时重新附加同一只读 ISO；单独导入的 JS 引擎模块仍是开发依赖声明，不能写成完整发布真实性认证。
  - [ ] 将声明版本绑定至经过验证的正式资源清单；独立磁盘增量、加密持久化和完整备份尚未证明，不能勾选 VM-004。摘要不是真实性认证。
- [ ] 记录启动耗时、浏览器实际内存/CPU、浏览器版本、引擎/镜像摘要及失败矩阵到 `design/browser-vm/g0-evidence.md`；不将 Node 探针当作浏览器运行证据。
  - [x] 已分别记录 Node 与浏览器 Worker 实测、真实取消、启动时间、快照字节、页面心跳及失败原因。
  - [ ] 峰值/稳态 CPU、实际进程内存、长时运行与设备覆盖仍待验收；配置内存和页面心跳不能替代这些数据。

### Task 3：受控网络与浏览器验证

- [x] 阅读固定 v86 网络适配实现，定义首帧授权包装的真实连接时序。测试未认证、错误票据或过期票据都不能触发目标 TCP 连接。
- [x] 实现仅本地验证用途的受控中继适配入口；显式允许测试仓库/API 域名，拒绝私网和任意目标。仅绑定随机分配的回环端口，不干扰现有开发服务器；测试单次票据、DNS 固定地址、断线禁止自动重连及并发边界。
- [ ] 在真实 Linux 内通过受控中继运行证书校验开启的 HTTPS 请求、HTTPS Git 拉取及包安装，保存退出码、目标与版本；不在记录中保留响应敏感正文。
  - [x] Node 真实 Linux 已通过首帧授权中继安装 Git/curl 并执行版本检查；本地控制路径自动测试通过。
  - [x] 后续一次 Node 真实 Linux 完整联网验证通过：证书校验开启的 Git 克隆、GitHub API HTTP 200，以及新建离线引擎恢复；保留较早 TLS EOF 失败记录，不将单次成功视为稳定验收。
  - [ ] Node 后续复测仍在包安装超时；浏览器完整联网和恢复后重新授权仍未通过。
- [ ] 在 `tools/browser-vm/index.html` 的本地专用验证页运行同一真实引擎，验证页面内终端输入、文件读写、快照恢复及联网；该页面不接入生产导航。
  - [x] 独立 module Worker 内真实运行和恢复；取消/加载错误/超时终止 Worker；浏览器内 Alpine/apk 已通过。
  - [x] 本地诊断页提供显式联网开关，仅在启用 Alpine 验证后允许开启；票据仅进入独立 Worker，生产页面和 CSP 不变。
  - [x] 完整 ISO 选择器、浏览器内离线启动/文件/恢复、实际取消和取消后重新运行通过；ISO 与 chroot 选项互斥，未配置资源时拒绝运行。
  - [ ] 浏览器联网安装分别在 79%、26%、57% 进度超过 90 秒命令期限；最后一次文档始终可见、100ms 心跳最大间隔 103ms，不能归因于后台页面。包安装完成、Git/API 仍待验收。此页仍是诊断工具，不是已交付产品页面。
  - [x] 离线人工输入单独验收：浏览器真实中文文件写入/读取、300 秒前台进程提前中断、后续命令、停止禁用输入、启动中取消，以及重启后旧临时文件不存在均通过。
  - [x] 添加分阶段耗时、文档可见性及只读中继/客体缓冲诊断；不输出票据/报文正文、不增加 HTTP 诊断入口、不延长超时。
- [ ] 断开网络再恢复检查点，验证重新鉴权且原命令不重放；检查旧 TCP 会话不被当作成功恢复。
  - [x] Node 真实完整 Alpine + 本地 HTTP 目标验证：保存活动连接后销毁，离线新引擎恢复不建连接；显式新票据后新请求成功；旧请求实际失败、旧 POST 仅执行一次、旧 TCP 已关闭。仅替换上游 DNS/dial 测试边界，不代表公网 TLS/Git/包或浏览器验收。
  - [x] 修复新引擎恢复不保留 NE2K MAC/设备内存的问题；快照兼容性摘要纳入该设备配置，拒绝旧资源摘要契约。失败复现及修复回归见 G0 账本。
  - [ ] 浏览器内断网/恢复/显式新授权、真实公网目标和正式成员授权仍需验证。
- [ ] 对照 VM-001～006 逐项登记实测结果。不能通过时保留具体失败和所需外部条件，不推进正式功能页面。
  - [x] 2026-09-10 更新 G0 证据：62 项快速测试、2 项真实离线 Linux 测试、类型检查与前端构建通过；真实联网测试失败独立记录，G0 不勾选完成。
  - [x] 同日后续诊断批次：66 项快速测试、类型检查通过；新增真实联网复测仍失败，记录收发字节与宿主直连对照，不覆盖前批次证据。
  - [x] 同日检查点批次：74 项快速测试、2 项真实 Node 离线 Linux 测试、类型检查通过；浏览器 Worker 的 Buildroot＋Alpine 用户空间恢复通过。官方 Alpine 整套镜像另有 Node 启动/文件/恢复实验，联网未在本批复测，旧失败不清除。
  - [x] 同日完整镜像接入批次：84 项快速测试、3 项真实 Node 离线 Linux 测试、类型检查通过；完整 ISO 浏览器启动约 17.4 秒，文件/恢复及取消后重跑通过。完整 ISO Node 联网安装在首个依赖包 1% 处超过 90 秒；宿主直连及宿主经同一中继的公开下载对照独立记录，后者无 VM 时也出现 TLS 握手超时，联网验收仍不通过。
  - [x] 同日交互串口批次：100 项快速测试、4 项真实 Linux 回归和类型检查通过；实际浏览器离线交互及重启清理证据见 G0 账本。修复启动后关闭竞态的未处理拒绝；中断测试改为等待真实进程标记而非输入回显。未重跑完整构建或联网验收，G0 保持未完成。
  - [x] 同日网络恢复批次：100 项快速测试、5 项真实 Linux 回归和类型检查通过；修复网卡恢复并验证旧快照拒绝、恢复默认离线、显式新授权和旧请求不重放。公网包索引仍在 90 秒期限失败，宿主 IPv4 直连也在 30 秒失败，分别记录；本地 HTTP 恢复成功不解除公网或 G0 门槛。

## 阶段检查与后续接续规则

- 每个阶段提交前执行对应自动化、`git diff --check` 并检查暂存范围；只提交本任务文件，保留已有未跟踪文件。
- G1～G5 的细化计划必须给出代码接口与失败用例，至少覆盖越权、幂等冲突、并发失效、存储失败和真实 UI 行为，不以零断言测试或 mock 成功替代。
- 生产中继位置、维护和预算授权作为发布门槛独立保留；本地开发并不包含任何自动购买/部署授权。
- 最终逐项回看完整规格而非只看本计划的勾选；任何未证明的要求都视为尚未完成。
