# 成熟度登记补齐：七个工作区路由与项目时间线

基线：审计页修复已提交 `5ebdaa0`；沿用 `codex/admin-audit-recovery`，不合并、不推送、不部署。

## 问题与边界

原始基线：`verify:workbench-maturity` 的 ready 路由覆盖合同为 12 通过 / 1 失败：goals、projects、calendar、today、focus、review、inbox 缺少能力登记。旧 R0 的 24 条能力记录同时参与运行时矩阵、操作根、API/持久化证据以及带指纹的缺口分派；不能只补七条声明，或减少测试枚举范围来通过门禁。

2026-09-14 执行发现：补齐菜单路由后，同一覆盖断言进一步发现独立参数化路由 `project-timeline` 也未登记；将其纳入相同只读审计范围，补齐四态、操作链路和缺口分派，不改业务逻辑或缩减 registry。M01 已提交 `e246fca`。

本次检查点 M01 先验证真实 App 路由及其读取边界，并提供逐页证据；不把现有静态页面测试当作完整交互验收。M02 再一次性同步关联合同，避免把部分登记当作清单闭环。

## M01：路由证据（当前检查点）

- [x] 提交 D02 审计页修复，复跑其 126 项定向测试。
- [x] 复现当前成熟度门禁缺口，保持原断言不变。
- [x] 阅读七页 route/page/data 及现有测试，区分静态渲染、真实路由和 Worker 证据。
- [x] 为七页验证 loading → 响应数据、初次失败 → 点击重试 → 恢复、空数据行为。
- [x] 验证无 `workspace.tasks` 权限时直接访问被拒绝，且不调用业务接口。
- [x] 验证四个 cursor 列表的加载更多确实追加下一页；记录与数字分页要求的差距。
- [x] 运行定向和相关 Worker 回归；以故障回注证明路由测试能捕获丢弃成功响应。
- [x] 同步证据、ROADMAP 和逐页未完成项；不提升 release / acceptance。

结果：32 项新增真实 App 读取测试 + 17 项对应 Worker 回归通过；故障回注先失败、还原后恢复通过。详见[逐页证据与原子缺口](../../product/2026-09-13-workbench-extended-route-audit.md)。M01 完成不代表 M02 门禁已修复。

## M02：登记与合同（2026-09-14 本地完成）

- [x] `M02-01` 在能力清单补齐七页及项目时间线路径、权限、旅程和证据；保留 partial 及未验证维度。
- [x] `M02-02` 八个入口加入统一四态矩阵、权限投影和导航 fixture；区分 Focus 新会话表单、Today/Review 局部空态与列表空态。
- [x] `M02-03` 补齐 frontend operation roots、实际 API/持久化链路、member_id 来源及可达 mutation；不把后端 client key 等同于前端重试稳定性。
- [x] `M02-04` 逐项绑定真实 mutation 安全证据；新增 18 项未验证并发/重试操作进入缺口分派，禁止无证据标 proven。
- [x] `M02-05` 更新 domain audit、带指纹 gap matrix、独立风险策略、负责人 atom 和阶段计数；保留历史 R0 证据时间范围。
- [x] `M02-06` 成熟度、域审计、交付状态、完整测试全部通过后提交本检查点；浏览器/生产验收仍独立。

结果与命令见[本地对账证据](../../operations/evidence/2026-09-14-workbench-m02-completion.md)：32 项能力、83 项缺口、26 个新增待实施原子；旧 R0 快照保持 24 项能力范围。

之后恢复既定 D01 管理仪表盘权威统计；以下逐页产品缺口另按优先级实施，不混入登记修复。
