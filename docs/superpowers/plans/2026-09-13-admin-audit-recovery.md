# D02：审计页加载与恢复闭环

基线：main `6bb04ac`。范围仅为审计日志读取；不新增 Cloudflare 服务、迁移或写操作，不调整其他管理页，也不部署。

根因：`createAdminAuditRequestController().request()` 返回 `{ generation, promise }`，promise 直接返回分页数据；路由却把分页数据解构为 `{ generation, page }`，成功响应不能进入 ready。错误态没有可用的原查询重试入口。

## 原子级 checklist

- [x] 核对 main、干净状态和现有管理分页基线（2 文件 / 17 测试）。
- [x] RED：通过真实路由断言列表和空态展示，替换仅检查 URL 的薄弱测试；最终版测试通过故障回注再次验证可捕获原阻断。
- [x] 修复成功响应处理；保留 generation、取消和卸载保护。
- [x] RED → GREEN：首次加载错误和换页错误可按原查询手动重试；请求中禁止重复操作。
- [x] 验证数字分页、每页条数、筛选归第一页和 URL 前进/后退同步。
- [x] 验证迟到成功/失败、导航瞬间结果和卸载取消，不污染当前视图。
- [x] 将成熟度矩阵中审计 ready/empty/error 从 gap 升级为 supported。
- [x] 回归服务端审计分页与权限，执行全量测试、构建和类型检查；范围与计数见本批证据。
- [x] 同步 ROADMAP、增量审计、入口矩阵及本批证据；保留 D02 其他管理页与浏览器/发布验收未完成状态。
- [ ] 集成前置：处理 `verify:workbench-maturity` 的既有失败（main 同样复现，7 个 ready 路由缺能力记录）；本批审计修复不将该缺口计为已完成。

## 验收边界

本地自动化通过不等于真实浏览器或生产验收。审计为既有管理员治理接口，保持原权限边界；不扩大普通用户可见范围，不将管理员审计改为私有用户业务列表。
