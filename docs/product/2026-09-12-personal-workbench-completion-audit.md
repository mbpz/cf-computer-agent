# 个人工作台：功能补齐增量审计

日期：2026-09-12；增量更新：2026-09-13。原基线：main `e46c512` 加当时未提交的 Shell 与产品定位修复；该修复随后提交为 `937ea87`。

用户已确认按对账、知识闭环、执行协作、管理/VM、验收的方向继续。本文不是全部功能审计完成报告。沿用既有成熟度清单和交付总账，避免重复建设；历史 checkbox 不能直接推断当前状态。

增量成果：[32 个登录后页面与 VM 历史对账](./2026-09-12-personal-workbench-route-inventory.md)；首个隐私修复批次为 [B02 成员作用域草稿计划](../superpowers/plans/2026-09-12-member-scoped-submission-drafts.md)。两组基线测试合计 14 files / 329 tests 通过，其中 gap 断言确认的是现有缺口，不是功能完成。

## 已确认的缺口

| 项目 | 当前代码证据 | 判定与下一步 |
| --- | --- | --- |
| 附件上传入口 | `frontend/pages/submit-page.tsx` 调用 `<AssetDropzone locale={locale} />`；组件 `enabled=false` | 主提交页未接上传闭环；不能仅解除 disabled，必须核对服务端能力、授权、容量和真实上传回调 |
| 多文件选择 | `frontend/components/assets/asset-dropzone.tsx` 使用 multiple，校验模型默认 maxFiles=1，调用未传上限 | 即使开启组件，多文件也会被直接拒绝；需统一数量限制并显示逐项失败原因 |
| AI 会话恢复 | `frontend/app.tsx` AgentRoute 的 conversationId 仅在 ref 中，AgentPage 只渲染当前问题与答案 | 当前页面没有历史列表/恢复入口；需核对现有后端会话接口后接入，不另造平行存储 |
| AI 范围选择 | AgentRoute 从 URL 解析 scope；`frontend/pages/agent-page.tsx` 只显示 scopeLabel | 支持范围参数不等于用户能选择范围；需来源选择、撤权反馈与会话范围一致性 |
| 顶部未读反馈 | AppShell 仅渲染协作链接；`frontend/lib/notifications-data.ts` 已有 `/api/notifications/summary` | 补接现有摘要并同步页面已读动作；不得将“未能加载”显示成“零未读” |
| 设置范围 | `frontend/pages/settings-page.tsx` 只有账户只读信息、主题和语言 | 当前基础设置可用；会话管理等扩展仍需接口与产品规则，不以新增空按钮交付 |
| 清单与入口覆盖 | 成熟度清单更新于 2026-09-01，记录 21 个菜单路由；当前共享 registry 已有 today/focus/review/inbox/goals/projects 等条目 | 旧审计范围滞后；重新建立当前全路由/操作矩阵，不能直接把旧 98 个待办当作当前缺失数 |
| 浏览器 Linux VM | main 已含 `tools/browser-vm`、`src/routes/environments.ts` 与 deletion reconciler；32 个页面仍无 VM 入口 | 历史基础实现已追溯，当前是工具/探针与元数据 API，需复用接入正式页面，不重复建设 |
| 离线草稿隐私 | B02 已采用认证成员 v2 命名空间、成员键控表单和迟到响应保护；旧无主 v1 不读不迁移 | 已在本地 main 合并（`6f9d325`）；[本地证据与验收边界](./2026-09-13-member-scoped-submission-drafts-evidence.md)，真实双账号与生产验收仍开放 |
| 提交重试 | `submission-intent.ts` 成员作用域持久化身份与快照；`createSubmission` 必须接收显式 key；页面手动恢复原请求 | B01 已合入 main `6bb04ac`；[证据与限制](./2026-09-13-stable-submission-intents-evidence.md)。真实刷新/双账号、移动端与生产验收仍开放 |
| 管理概览 | `frontend/app.tsx` 向 AdminDashboardPage 传三个写死的 0 | D01：接真实授权数据并区分 loading/error/empty，不显示虚假零值 |
| 审计页加载与恢复 | AdminAuditRoute 使用 request.generation 与即时查询校验，成功结果直接作为 page；错误可原查询重试 | D02 审计子项本地修复，ready/empty/error 已升级 supported；[证据与限制](./2026-09-13-admin-audit-recovery-evidence.md)。其他管理页、浏览器及生产验收仍开放 |

## 统一完成标准

每个功能独立记录 implementation / verification / release / acceptance。只有“入口 → 输入校验 → 授权 API → 持久化 → 返回反馈 → 刷新回读”完整，才可称本地流程完成。

每项实施必须覆盖：服务端成员隔离、越权直达拒绝、适用列表分页、稳定幂等键、重复请求、并发冲突、取消/重试、空态/错误态、键盘与移动端，以及具体测试/浏览器证据。无后端能力的操作不得静默成功。部署与生产数据操作需另行授权。

## 已确认方向与原子清单（逐批细化）

隐私和用户阻断优先：B02 → B01；审计页与真实统计提前至 D02/D01 小批修复。其余仍按知识闭环、执行协作、管理与 VM、验收顺序推进。下面任务未完成全部子项时保持未勾选。

### A — 当前能力对账与基础交互

- [ ] A01 枚举当前所有静态/参数化路由与可见操作，包括 VM 历史实现追溯。
  - [x] 静态 28 + 参数化 4 个页面枚举及 VM main 历史追溯。
  - [ ] 每页全部可见操作、弹窗二级操作与触发状态枚举。
- [ ] A02 每个操作绑定前端 handler、API、授权检查、持久化与测试证据。
- [ ] A03 分类已实现待验收、未接线、部分实现、未实现、受资源限制。
- [ ] A04 将本轮账户/导航/品牌修复映射到原 R1 条目，仅提升已有证据的子项。
- [ ] A05 逐页核对弹层、危险操作确认、表单 pending/dirty、重复点击和异步状态。
- [ ] A06 验证全局摘要真实数据、未读同步、错误重试及页面退出后的请求清理。

### B — AI 知识库核心闭环（首个业务批次）

- [ ] B01 验证文本/Markdown/code 提交、稳定幂等键及失败重试。
  - [x] 成员意图持久化、显式幂等键、手动恢复、新编辑保留、损坏/不可用存储反馈；见 [B01 证据](./2026-09-13-stable-submission-intents-evidence.md)。
  - [x] 真实路由 DOM/Response 自动化与本地 D1 成员隔离/精确重放验证。
  - [ ] 真实浏览器刷新/双账号、中英文键盘与移动端、发布后验收；不以 DOM fixture 替代。
- [ ] B02 草稿恢复按成员隔离，切换账户/退出不暴露他人草稿。
  - [x] 成员存储、身份接线、异步生命周期及自动化回归；合入本地 main `6f9d325`，见 [B02 证据](./2026-09-13-member-scoped-submission-drafts-evidence.md)。
  - [ ] 真实浏览器双账号退出/登录验收与生产验收；不以 fixture 身份代替。
- [ ] B03 核对存储 bindings 与免费层边界，确定附件可用性返回契约。
- [ ] B04 接入附件选择、数量/大小/类型校验及可见错误。
- [ ] B05 接入真实上传、进度、取消、失败重试与解析任务回读。
- [ ] B06 审核、发布与重复发布回放贯通，错误不产生可见半成品。
- [ ] B07 知识列表过滤/分页 → 阅读 → 精确引用回读贯通。
- [ ] B08 AI 来源范围选择、会话恢复、取消/重试与证据不足反馈贯通。
- [ ] B09 知识关联任务及讨论的跨模块跳转重新校验权限。

### C — 个人执行与协作

- [ ] C01 任务创建/编辑/状态/截止时间/标签/知识关联逐项验收。
- [ ] C02 看板与任务共用权威数据，验证分页、键盘移动、并发与回滚。
- [ ] C03 目标/项目的任务归属、进度与时间线计数对账。
- [ ] C04 收集箱/今日/日历/专注/复盘逐条核对真实流程，不重复新建已有实现。
- [ ] C05 通知过滤、已读/批量已读、重复事件及失效目标处理。
- [ ] C06 上下文消息列表/详情/发送/分页/重试与撤权处理；不扩展为通用私聊。
- [ ] C07 核对删除、恢复、保留期与清理是否已有实现；缺失部分单独设计。

### D — 管理、VM 与发布验收

- [ ] D01 管理仪表盘/统计数据与业务计数、日期范围、分页一致。
- [ ] D02 审核/资产/成员/角色/菜单/空间/审计各页主操作逐项跑通。
  - [x] 审计成功/空态、数字分页/筛选、原查询重试、重复点击与迟到响应保护本地实现及定向回归；见 [D02 审计原子清单](../superpowers/plans/2026-09-13-admin-audit-recovery.md)。
  - [ ] 其余管理页初始失败重试及各主操作闭环，逐页细化后执行。
  - [ ] 审计真实管理员/普通成员、中英文键盘/设备及发布后验收。
- [ ] D03 知识版本、回收、导出/恢复及研究产物按原 roadmap 对账，确定未完成子项。
- [ ] D04 VM 历史实现确认后补齐启动/停止/恢复、终端、存储与联网的未完成项。
- [ ] D05 admin/contributor/第二成员正向与越权旅程验收。
- [ ] D06 320/375/768/1280、中英文、深浅主题、键盘/触控矩阵验收。
- [ ] D07 运行精确候选完整门禁；同步 README、ROADMAP、总账和原 checklist。
- [ ] D08 授权后执行迁移/部署与真实登录验收，单独记录证据和回滚点。

以上任务先做现有实现对账，再细化到文件、请求、数据字段与独立测试；不得凭条目名称勾选完成。
