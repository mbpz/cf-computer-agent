# B08 会话历史列表 — 既有功能本地执行切片

沿用已有 conversation 表与单会话恢复入口；不增加生产迁移或平行存储。

- [x] RED：会话列表严格 owner-bound、稳定创建顺序、同时间 ID 断点、cursor 成员绑定与非法参数拒绝。
- [x] GET 列表仅返回 id/createdAt，不暴露历史正文或来源标题；打开单会话继续使用现有引用重新授权接口。
- [x] 显式打开历史列表，20 项一页、前后页与重新加载；错误清除旧页、重试原页，导航/卸载忽略晚到响应，不自动发问。
- [x] 数据响应严格校验及 DOM/Worker 定向回归；更新实际 API/pagination 证据，保持父项验收未完成。
- [x] 本地构建/类型/双语/领域/交付契约，更新 checklist，提交；不 push/deploy/远程迁移。

按 immutable created_at DESC, id DESC 分页，后续回答不移动位置；新增会话需重新加载首页。不声称数据库时间旅行快照、问题回合幂等或原生浏览器验收。
