# B08 问题回合幂等 — 本地执行切片

沿用已有 chat 会话和消息。新增本地迁移仅建立请求收据/唯一键，不改生产数据库，不部署。旧无键 API 暂保留兼容，不声称其幂等。

- [x] RED：同成员同键/同载荷仅生成一次，同键不同载荷 409，不同成员隔离，重放重新鉴权。
- [x] 持久请求收据 `(member_id, idempotency_key)` 原子占位；pending 不重新执行，不用超时租约猜测失败；失败收据也不重跑 AI。
- [x] 同一 D1 batch 原子保存消息和完成收据；取消/失败不能缓存成功。未知提交结果保留并显式回读/同键重放。
- [x] 前端新回合生成稳定键，未知结果固定原载荷；成员分区 sessionStorage 保存未确认意图（不保存答案），刷新后只提示手动恢复，不自动 POST；存储不可用时禁止有持久恢复预期的新写入。
- [x] 明确放弃/Stop 释放当前意图，保留服务端取消未确认语义；账户切换不读取其他成员意图。
- [x] DOM/Worker/迁移定向回归、类型、构建、双语和交付契约；更新 checklist 后本地提交。

边界：持久收据保证带键请求至多一次进入生成，不能让外部 AI 和数据库成为分布式事务；进程中断留下 pending 只报告结果未知，不自动接管。不同标签独立新建意图不保证语义去重。生产应用迁移需另行授权。


## 本地验证（2026-09-26）

- Worker RED：原同键重试创建两个会话、非法 key 被接受；前端 RED：未发送 key、错误回执被显示；补充回执会话不匹配反例也先失败后修复。
- GREEN：14 文件 399/399，包括 Worker API、迁移、成员意图存储、DOM 恢复/取消、数据回执、既有引用/审核/无障碍及会话服务。
- `typecheck`、`build:ui`、双语静态检查通过；完整双语契约 13/13、成熟度契约 13/13、领域审计 26/26。交付契约结果见同日进度记录。
- 新增 `0052_chat_turn_requests.sql` 只在本地隔离测试应用；未推送、部署或对生产数据库执行迁移。
- pending 可永久未知，失败回执不会重跑；明确放弃可能产生独立新请求，不保证分布式 exactly-once。无键兼容路径和跨标签独立新意图仍无语义去重保证。原生浏览器和真实身份验收另列，不由 happy-dom 替代。
