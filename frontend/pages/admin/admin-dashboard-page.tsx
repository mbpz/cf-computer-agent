import { Card, CardContent, CardHeader, CardTitle } from "../../components/ui/card";
import { frontendText, type LocaleRuntime } from "../../lib/i18n";

export function AdminDashboardPage({ metrics, locale }: { metrics: { pending: number; assets: number; members: number }; locale?: LocaleRuntime }) {
  return <section className="space-y-6"><div><p className="text-sm font-medium text-primary">{frontendText(locale, "ADMIN_EYEBROW")}</p><h1 className="mt-2 text-2xl font-semibold">{frontendText(locale, "ADMIN_TITLE")}</h1><p className="mt-1 text-sm text-muted-foreground">{frontendText(locale, "ADMIN_DESCRIPTION")}</p></div><div className="grid gap-4 md:grid-cols-3">{[[frontendText(locale, "ADMIN_REVIEW_QUEUE"), metrics.pending], [frontendText(locale, "ADMIN_ASSET_QUEUE"), metrics.assets], [frontendText(locale, "ADMIN_MEMBERS"), metrics.members]].map(([label, value]) => <Card key={label as string}><CardHeader><CardTitle className="text-sm font-medium text-muted-foreground">{label}</CardTitle></CardHeader><CardContent><p className="text-3xl font-semibold tabular-nums">{value}</p></CardContent></Card>)}</div><Card><CardHeader><CardTitle>{frontendText(locale, "ADMIN_QUICK_LINKS")}</CardTitle></CardHeader><CardContent className="grid gap-2 sm:grid-cols-3"><a href="/admin/analytics" className="rounded-md border px-3 py-2 text-sm hover:bg-accent">{frontendText(locale, "NAV_SITE_ANALYTICS")}</a><a href="/admin/roles" className="rounded-md border px-3 py-2 text-sm hover:bg-accent">{frontendText(locale, "NAV_ROLES")}</a><a href="/admin/menus" className="rounded-md border px-3 py-2 text-sm hover:bg-accent">{frontendText(locale, "NAV_MENUS")}</a></CardContent></Card></section>;
}
